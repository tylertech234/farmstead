# scripts/ — helper shell scripts
#
# Place helper scripts here, for example:
#
#   backup.sh    — dump named Docker volumes to dated tar archives
#   restore.sh   — restore volumes from a backup archive
#   pull-model.sh — pull the Ollama model specified in .env
#
# Example usage (run from the repository root):
#   bash scripts/backup.sh
